# Error Patterns & Fixes

Place error patterns and their fixes here as `.md` files.

Agents read these to avoid repeating past mistakes. Examples: "Merge conflict in shared types file — fix by giving each worker non-overlapping type files" or "CDK bootstrap required before first deploy."
