# Reusable Patterns

Place reusable code patterns and conventions here as `.md` files.

Agents read these to follow established patterns instead of inventing new ones. Examples: "Lambda handler boilerplate", "DynamoDB single-table access pattern", "Error response format."
