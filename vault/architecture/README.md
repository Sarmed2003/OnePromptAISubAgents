# Architecture Decisions

Place architecture decision records here as `.md` files.

Agents read these during planning to understand past architectural choices that worked well. Examples: "We chose DynamoDB single-table design for the user service" or "Event-driven via EventBridge worked better than direct Lambda-to-Lambda calls."
