/** DINOLAB + repo metadata shown on the hosted (Vercel) research console. */
export const PROJECT = {
  appName: "DINOLAB",
  subtitle: "Bone Detective Lab",
  /** GitHub repository that ships this UI (OnePromptAI sub-agents / DINOLAB). */
  githubRepo: "Sarmed2003/OnePromptAISubAgents",
  /** Primary production hostname on Vercel. */
  vercelHost: "dinolab.vercel.app",
  /** Parent orchestrator stack name (for copy). */
  orchestrator: "OnePromptAI",
} as const;
